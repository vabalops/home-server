# helm install envoy-gateway oci://docker.io/envoyproxy/gateway-helm --version v1.6.1 -n envoy-gateway-system --create-namespace --values ./envoy-values.yaml
# helm upgrade envoy-gateway oci://docker.io/envoyproxy/gateway-helm --version v1.6.1 -n envoy-gateway-system --values ./envoy-values.yaml

config:
# -- EnvoyGateway configuration. Visit https://gateway.envoyproxy.io/docs/api/extension_types/#envoygateway to view all options.
  envoyGateway:
    gateway:
      controllerName: gateway.envoyproxy.io/gatewayclass-controller
    provider:
      type: Kubernetes
    logging:
      level:
        default: info